@model IList<CategoryModel>

@using Nop.Web.Models.Catalog;
@if (Model.Count > 0)
{
    <section class="hp-primary-content">
        <h2>@T("HomePage.Destinations")</h2>
        @Html.Partial("_FeatureDestinations", Model)
    </section>
}
